/* ---------- Announcements / inbox / groups ---------- */
async function fetchAnnouncements(){try{return (await api('/api/v18/announcements')).announcements||[]}catch(_){return[]}}
function appendHomeAnnouncements(){fetchAnnouncements().then(rows=>{const sec=$('#view .section');if(!sec||!rows.length||$('#v18Announcements'))return;const d=document.createElement('div');d.id='v18Announcements';d.className='card';d.innerHTML=`<div class="row between"><h3 class="title" style="margin:0">پیام‌های مدیریت</h3><button class="chip" onclick="openV18Inbox()">همه</button></div><div style="margin-top:9px">${rows.slice(0,2).map(a=>`<div class="announcementCard"><h4>${esc(a.title)}</h4><p>${esc(a.body)}</p><small>${esc(a.author||'مدیریت')} · ${faDate(String(a.created_at).slice(0,10))}</small></div>`).join('')}</div>`;const hero=sec.querySelector('.homeHero,.hero');if(hero)hero.insertAdjacentElement('afterend',d);else sec.prepend(d)})}
const _home=home;home=function(){_home();appendHomeAnnouncements();installHeaderTools()};
window.openV18Inbox=async function(){let rows=[];try{rows=(await api('/api/v18/notifications')).notifications||[]}catch(_){}const unread=rows.filter(x=>!x.read_at);modal('اعلان‌ها',`${isManager()?`<button class="btn" onclick="openAnnouncementComposer()">+ پیام به تیم</button><div class="divider"></div>`:''}<div class="list">${rows.length?rows.map(n=>`<div class="item"><div class="row between"><strong>${esc(n.title)}</strong><span class="badge ${n.read_at?'':'gold'}">${n.read_at?'خوانده':'جدید'}</span></div><small>${esc(n.body)}<br>${faDate(String(n.created_at).slice(0,10))}</small></div>`).join(''):'<div class="empty">اعلانی نیست.</div>'}</div>`);if(unread.length){api('/api/v18/notifications/read',{method:'POST',body:JSON.stringify({ids:unread.map(x=>x.id)})}).catch(()=>{});const dot=$('#v18NotifDot');if(dot)dot.classList.add('hidden')}};
window.openAnnouncementComposer=function(){modal('ارسال پیام مدیریت',`<div class="field"><label>عنوان</label><input id="ann_title" placeholder="مثلاً تبریک فروش هفته"></div><div class="field"><label>متن پیام</label><textarea id="ann_body"></textarea></div><div class="field"><label>واحد</label><select id="ann_unit">${isInternal()?'<option value="academy">آکادمی کاشانی</option><option value="marshall">تیم مارشال</option>':`<option value="${user().division}">${divisionNames[user().division]}</option>`}</select></div><button class="btn" onclick="sendAnnouncementV18()">ارسال به کل تیم</button>`)};
window.sendAnnouncementV18=async function(){const title=$('#ann_title').value.trim(),body=$('#ann_body').value.trim(),business_unit=$('#ann_unit').value;if(!title||!body)return toast('عنوان و متن را کامل کن');try{await api('/api/v18/announcements',{method:'POST',body:JSON.stringify({title,body,business_unit,audience:'all'})});closeModal();toast('پیام برای تیم ارسال شد')}catch(_){toast('ارسال انجام نشد')}};
function startNotificationPolling(){if(v18NotifTimer)clearInterval(v18NotifTimer);const check=async()=>{if(!apiToken)return;try{const rows=(await api('/api/v18/notifications')).notifications||[],unread=rows.filter(x=>!x.read_at);const dot=$('#v18NotifDot');if(dot)dot.classList.toggle('hidden',!unread.length);for(const n of unread.slice().reverse()){if(Number(n.id)>v18LastNotif){v18LastNotif=Math.max(v18LastNotif,Number(n.id));if(window.AndroidBridge){try{AndroidBridge.notify(n.title,n.body,String(n.id))}catch(_){}}}}}catch(_){}};check();v18NotifTimer=setInterval(check,25000)}
window.openV18Groups=async function(){clearInterval(v18ChatTimer);let gs=[];try{gs=(await api('/api/v18/groups')).groups||[]}catch(_){}modal('گروه‌های تیم',`${isManager()?`<button class="btn" onclick="openGroupCreatorV18()">+ ساخت گروه تیمی</button><div class="divider"></div>`:''}<div class="chatList">${gs.length?gs.map(g=>`<button class="v18MenuItem" onclick="openGroupChatV18(${g.id},'${esc(g.name).replace(/'/g,"\\'")}')"><b>${esc(g.name)}</b><span>${fmt((g.members||[]).length)} عضو</span></button>`).join(''):'<div class="empty">هنوز گروهی برای شما ساخته نشده.</div>'}</div>`)};
window.openGroupCreatorV18=async function(){await refreshEmployees();const unit=isInternal()?'academy':user().division,active=v18Employees.filter(x=>x.active&&x.business_unit===unit);modal('ساخت گروه',`<div class="field"><label>نام گروه</label><input id="grp_name" placeholder="مثلاً تیم فروش"></div>${isInternal()?`<div class="field"><label>واحد</label><select id="grp_unit" onchange="groupUnitChangedV18()"><option value="academy">آکادمی</option><option value="marshall">مارشال</option></select></div>`:`<input type="hidden" id="grp_unit" value="${unit}">`}<div id="grp_members"><div class="sectionLabel">اعضا</div>${active.map(e=>`<label class="item" style="display:flex;align-items:center;gap:8px"><input type="checkbox" class="grpMember" value="${e.id}" style="width:auto"><span>${esc(e.full_name)} · ${esc(roleNames[e.role]||e.role)}</span></label>`).join('')}</div><button class="btn" style="margin-top:9px" onclick="createGroupV18()">ساخت گروه</button>`)};
window.groupUnitChangedV18=function(){const unit=$('#grp_unit').value,active=v18Employees.filter(x=>x.active&&x.business_unit===unit),box=$('#grp_members');box.innerHTML=`<div class="sectionLabel">اعضا</div>${active.map(e=>`<label class="item" style="display:flex;align-items:center;gap:8px"><input type="checkbox" class="grpMember" value="${e.id}" style="width:auto"><span>${esc(e.full_name)} · ${esc(roleNames[e.role]||e.role)}</span></label>`).join('')}`};
window.createGroupV18=async function(){const name=$('#grp_name').value.trim(),business_unit=$('#grp_unit').value,member_ids=$$('.grpMember:checked').map(x=>Number(x.value));if(!name)return toast('نام گروه را وارد کن');try{await api('/api/v18/groups',{method:'POST',body:JSON.stringify({name,business_unit,member_ids})});openV18Groups();toast('گروه ساخته شد')}catch(_){toast('ساخت گروه انجام نشد')}};
window.openGroupChatV18=async function(id,name){clearInterval(v18ChatTimer);const render=async()=>{try{const ms=(await api(`/api/v18/groups/${id}/messages`)).messages||[],box=$('#v18ChatMessages');if(!box)return;const atBottom=box.scrollHeight-box.scrollTop-box.clientHeight<80;box.innerHTML=ms.map(m=>`<div class="chatBubble ${Number(m.user_id)===Number(serverIdOf(user()))?'mine':''}"><small>${esc(m.full_name)}</small><div>${esc(m.body)}</div></div>`).join('')||'<div class="empty">پیامی نیست.</div>';if(atBottom)box.scrollTop=box.scrollHeight}catch(_){}};modal(name,`<div id="v18ChatMessages" style="height:48vh;overflow:auto"></div><div class="chatComposer"><input id="v18ChatInput" placeholder="پیام..." onkeydown="if(event.key==='Enter')sendGroupMessageV18(${id})"><button onclick="sendGroupMessageV18(${id})">➤</button></div>`);await render();v18ChatTimer=setInterval(render,7000)};
window.sendGroupMessageV18=async function(id){const el=$('#v18ChatInput'),body=el.value.trim();if(!body)return;el.value='';try{await api(`/api/v18/groups/${id}/messages`,{method:'POST',body:JSON.stringify({body})});const ms=(await api(`/api/v18/groups/${id}/messages`)).messages||[],box=$('#v18ChatMessages');if(box){box.innerHTML=ms.map(m=>`<div class="chatBubble ${Number(m.user_id)===Number(serverIdOf(user()))?'mine':''}"><small>${esc(m.full_name)}</small><div>${esc(m.body)}</div></div>`).join('');box.scrollTop=box.scrollHeight}}catch(_){toast('ارسال نشد')}};

