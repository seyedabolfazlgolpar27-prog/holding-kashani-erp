/* ---------- Report range center / Excel + PDF ---------- */
let reportPreset='6m';
function presetRange(p){const end=new Date(),start=new Date(end);if(p==='1m')start.setMonth(start.getMonth()-1);else if(p==='3m')start.setMonth(start.getMonth()-3);else if(p==='6m')start.setMonth(start.getMonth()-6);else if(p==='year'){const j=toJ(nowKey());return{from:toG(j.jy,1,1),to:nowKey()}}return{from:nowKeyFromDate(start),to:nowKeyFromDate(end)}}
function reportCenterHtml(){const r=presetRange(reportPreset),jf=toJ(r.from),jt=toJ(r.to);return `<div class="card reportCenter v18Panel"><div class="row between"><div><h3 class="title" style="margin:0">مرکز خروجی و آرشیو</h3><div class="muted">ماهانه، چندماهه، شش‌ماهه و سالانه</div></div><span class="tag">شمسی</span></div><div class="reportPresetGrid" style="margin-top:10px">${[['1m','۱ ماه'],['3m','۳ ماه'],['6m','۶ ماه'],['year','سال جاری']].map(([k,l])=>`<button class="reportPreset ${reportPreset===k?'active':''}" onclick="setReportPresetV18('${k}')">${l}</button>`).join('')}</div><div class="jalaliRange"><div class="jalaliDateBox"><label>از تاریخ</label>${jalaliPicker('rangefrom',r.from)}</div><div class="jalaliDateBox"><label>تا تاریخ</label>${jalaliPicker('rangeto',r.to)}</div></div><div class="exportButtons"><button class="btn" onclick="exportExcelV18()">خروجی Excel</button><button class="btn ghost" onclick="exportPdfV18()">گزارش PDF</button></div><div class="muted" style="margin-top:7px">سال‌های قبلی حذف نمی‌شوند؛ با انتخاب بازه، همان آرشیو تاریخی دوباره قابل خروجی است.</div></div>`}
window.setReportPresetV18=function(p){reportPreset=p;renderReports('month')};
function appendReportCenter(){const sec=$('#view .section');if(sec&&!$('#v18ReportCenter')){const box=document.createElement('div');box.id='v18ReportCenter';box.innerHTML=reportCenterHtml();sec.insertBefore(box,sec.firstChild)}}
const _renderReports=renderReports;renderReports=function(mode='month'){_renderReports(mode);appendReportCenter()};
window.exportExcelV18=async function(){if(!isManager())return toast('خروجی سازمانی فقط برای مدیریت است');const from=pickerISO('rangefrom'),to=pickerISO('rangeto'),unit=user().division;try{const res=await fetch(`/api/v18/export.xlsx?business_unit=${encodeURIComponent(unit)}&from=${from}&to=${to}`,{headers:{Authorization:'Bearer '+apiToken}});if(!res.ok)throw Error();const blob=await res.blob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`Holding-Kashani-${from}-${to}.xlsx`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),2000);toast('فایل Excel آماده شد')}catch(_){toast('ساخت Excel انجام نشد')}};
window.exportPdfV18=function(){if(!isManager())return toast('گزارش سازمانی فقط برای مدیریت است');const from=pickerISO('rangefrom'),to=pickerISO('rangeto'),regs=(state.registrations||[]).filter(r=>r.date>=from&&r.date<=to),reports=Object.entries(state.reports||{}).filter(([k])=>{const d=k.split('|')[1]||'';return d>=from&&d<=to}),months=[];let d=new Date(from+'T12:00:00'),e=new Date(to+'T12:00:00'),seen=new Set();while(d<=e){const k=pMonthKey(nowKeyFromDate(d));if(!seen.has(k)){seen.add(k);months.push(k)}d.setDate(d.getDate()+15)}const active=allUsers().filter(x=>x.division===user().division),rows=active.map(u=>{const sc=months.map(k=>{try{return coinScoreForMonth(u.id,k)}catch(_){return 0}}).filter(Number);return[u.name,roleNames[u.role]||u.role,sc.length?Math.round(sc.reduce((a,b)=>a+b,0)/sc.length):0]}).sort((a,b)=>b[2]-a[2]);const root=$('#printRoot');root.classList.remove('hidden');root.innerHTML=`<div class="printPage"><div class="printHeader"><h1>گزارش تجمیعی ${divisionNames[user().division]}</h1><p>${faDate(from)} تا ${faDate(to)}</p></div><div class="printSection"><h2>خلاصه بازه</h2><div class="printGrid"><div class="printMetric"><span>ثبت‌نام رسمی</span><b>${fmt(regs.length)}</b></div><div class="printMetric"><span>مبلغ فروش رسمی</span><b>${money(regs.reduce((s,x)=>s+Number(x.amount||0),0))}</b></div><div class="printMetric"><span>گزارش‌های روزانه</span><b>${fmt(reports.length)}</b></div><div class="printMetric"><span>ماه‌های گزارش</span><b>${fmt(months.length)}</b></div></div></div><div class="printSection"><h2>میانگین عملکرد نیروها در بازه</h2><table class="printTable"><thead><tr><th>نیرو</th><th>سمت</th><th>میانگین امتیاز</th></tr></thead><tbody>${rows.map(x=>`<tr><td>${esc(x[0])}</td><td>${esc(x[1])}</td><td>${x[2]}/100</td></tr>`).join('')}</tbody></table></div>${months.map(k=>monthlyTeamsHTML(k,true)).join('')}</div>`;const clean=()=>{root.classList.add('hidden');root.innerHTML='';window.removeEventListener('afterprint',clean)};window.addEventListener('afterprint',clean);setTimeout(()=>window.print(),180)};

