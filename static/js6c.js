function settingsCard(u){if(!settingsRoles.includes(u.role))return'';return `<div class="card"><div class="row between"><div><h3 class="title" style="margin:0">تنظیمات مدیریتی</h3><div class="muted">مدیریت دوره‌ها، قیمت‌ها و نیروهای سازمان</div></div><span class="tag">مدیر</span></div><div class="grid2" style="margin-top:12px"><button class="btn" onclick="openCourseSettings()">دوره‌ها و قیمت‌ها</button><button class="btn ghost" onclick="openEmployeeSettings()">افزودن نیرو</button></div></div>`}
function openCourseSettings(){const rows=state.courseCatalog.map(c=>`<div class="item"><div class="row between"><div class="grow"><strong>${c.name}</strong><small>${c.active===false?'غیرفعال':'فعال'} · ${c.price?money(c.price):'قیمت تعیین نشده'}</small></div><span class="badge ${c.active===false?'':'gold'}">${c.active===false?'غیرفعال':'فعال'}</span></div><div class="grid2" style="margin-top:10px"><div class="field" style="margin:0"><label>نام دوره</label><input id="cn_${c.id}" value="${c.name}"></div><div class="field" style="margin:0"><label>قیمت پایه</label><input id="cp_${c.id}" type="number" inputmode="numeric" value="${Number(c.price||0)}"></div></div><div class="grid2" style="margin-top:9px"><button class="btn sm" onclick="updateCourse('${c.id}')">ذخیره تغییرات</button><button class="btn sm ghost" onclick="toggleCourse('${c.id}')">${c.active===false?'فعال کردن':'غیرفعال کردن'}</button></div></div>`).join('');modal('مدیریت دوره‌ها و قیمت‌ها',`<div class="card"><h3 class="title">افزودن دوره جدید</h3><div class="field"><label>نام دوره / وبینار</label><input id="new_course_name" placeholder="مثلاً دوره جدید فروش"></div><div class="field"><label>قیمت پایه</label><input id="new_course_price" type="number" inputmode="numeric" placeholder="مثلاً 15000000"></div><button class="btn" onclick="addCourse()">+ افزودن دوره</button></div><div class="sectionLabel">دوره‌های فعلی</div><div class="list">${rows}</div><div class="notice" style="margin-top:12px">برای حفظ گزارش‌های قبلی، دوره حذف نمی‌شود؛ در صورت نیاز آن را غیرفعال کنید.</div>`)}
function addCourse(){const name=$('#new_course_name').value.trim(),price=Number($('#new_course_price').value||0);if(!name)return toast('نام دوره را وارد کن');if(state.courseCatalog.some(c=>c.name===name))return toast('این دوره قبلاً وجود دارد');state.courseCatalog.push({id:'course_'+Date.now(),name,price:Math.max(0,price),active:true});syncCatalogArrays();save();openCourseSettings();toast('دوره جدید اضافه شد')}
function updateCourse(id){const c=state.courseCatalog.find(x=>x.id===id);if(!c)return;const name=$('#cn_'+id).value.trim(),price=Number($('#cp_'+id).value||0);if(!name)return toast('نام دوره نمی‌تواند خالی باشد');if(state.courseCatalog.some(x=>x.id!==id&&x.name===name))return toast('نام دوره تکراری است');const oldName=c.name;c.name=name;c.price=Math.max(0,price);for(const r of state.registrations){if(r.course===oldName)r.course=name}syncCatalogArrays();save();openCourseSettings();toast('اطلاعات دوره ذخیره شد')}
function toggleCourse(id){const c=state.courseCatalog.find(x=>x.id===id);if(!c)return;c.active=c.active===false?true:false;syncCatalogArrays();save();openCourseSettings();toast(c.active?'دوره فعال شد':'دوره غیرفعال شد')}
function roleOptionsForDivision(div){return Object.entries(roleNames).filter(([k])=>div==='marshall'?k.startsWith('marshall_'):!k.startsWith('marshall_'))}
function employeeRoleSelect(){const div=$('#emp_division')?.value||'academy',el=$('#emp_role');if(!el)return;el.innerHTML=roleOptionsForDivision(div).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}
function openEmployeeSettings(){const div=user()?.division||'academy',custom=(state.customUsers||[]).filter(x=>x.division===div);modal('افزودن و مدیریت نیرو',`<div class="card"><h3 class="title">نیروی جدید</h3><div class="field"><label>نام و نام خانوادگی / عنوان نمایشی</label><input id="emp_name"></div><div class="grid2"><div class="field"><label>واحد</label><select id="emp_division" onchange="employeeRoleSelect()"><option value="${div}">${divisionNames[div]}</option></select></div><div class="field"><label>پوزیشن</label><select id="emp_role"></select></div></div><div class="grid2"><div class="field"><label>نام کاربری</label><input id="emp_username" autocomplete="off"></div><div class="field"><label>رمز عبور</label><input id="emp_password" autocomplete="new-password" value="" placeholder="رمز اختصاصی"></div></div><button class="btn" onclick="addEmployee()">+ افزودن نیرو</button></div><div class="sectionLabel">نیروهای اضافه‌شده توسط مدیریت</div><div class="list">${custom.length?custom.map(x=>`<div class="item row between"><div><strong>${x.name}</strong><small>${divisionNames[x.division]} · ${roleNames[x.role]} · ${x.username}</small></div><span class="tag">فعال</span></div>`).join(''):'<div class="empty">هنوز نیروی جدیدی اضافه نشده است.</div>'}</div><div class="info" style="margin-top:12px">نیروی جدید بلافاصله در صفحه ورود، گزارش‌های مدیریتی و انتخاب فرد برای Task نمایش داده می‌شود.</div>`);employeeRoleSelect()}
async function addEmployee(){const name=$('#emp_name').value.trim(),division=$('#emp_division').value,role=$('#emp_role').value,username=$('#emp_username').value.trim(),password=$('#emp_password').value;if(!name||!username||!password)return toast('نام، نام کاربری و رمز را کامل کن');if(allUsers().some(x=>x.username===username))return toast('این نام کاربری قبلاً استفاده شده');try{const d=await api('/api/users',{method:'POST',body:JSON.stringify({full_name:name,business_unit:division,role,username,password})});state.customUsers.push({id:'custom_'+(d.id||Date.now()),division,role,name,username});save();openEmployeeSettings();toast('نیروی جدید روی سرور ساخته شد')}catch(e){toast(e.message==='username_exists'?'این نام کاربری قبلاً استفاده شده':'ساخت کاربر روی سرور انجام نشد')}}
function more(){const u=user(),taskAdmin=accessibleUsers(u).length?`<div class="card"><h3 class="title">مدیریت وظایف</h3><button class="btn" onclick="openTask()">+ تعیین وظیفه برای نیرو</button><div class="muted" style="margin-top:8px">Taskهای به‌موقع مستقیماً در بخش امتیاز وظایف اثر می‌گذارند. Task بعد از Deadline سکه نمی‌دهد.</div></div>`:'';$('#view').innerHTML=`<section class="section">${themeCard()}${settingsCard(u)}${taskAdmin}<div class="card"><h3 class="title">درخواست‌ها</h3><div class="grid2"><button class="btn ghost" onclick="openRequest('مرخصی')">مرخصی</button><button class="btn ghost" onclick="openRequest('مساعده')">مساعده</button><button class="btn ghost" onclick="openRequest('وام')">وام</button><button class="btn ghost" onclick="openRequest('سایر')">سایر</button></div></div><div class="card"><h3 class="title">بازخورد سازمانی</h3><div class="grid2"><button class="btn ghost" onclick="openSuggestion()">📣 انتقادات و پیشنهادات</button><button class="btn ghost" onclick="openIdea()">💡 ایده خلاقانه</button></div></div><div class="card"><h3 class="title">حساب کاربری</h3><div class="profileAvatarCard">${avatarSvg(u.id,'avatarMd')}<div class="grow"><strong>${u.name}</strong><div class="muted">${roleNames[u.role]} — ${divisionNames[u.division]}</div><button class="chip" style="margin-top:7px" onclick="openAvatarCustomizer()">ساخت / ویرایش کاراکتر</button></div></div><button class="btn alt" style="margin-top:10px" onclick="logout()">خروج از حساب</button></div><div class="card"><h3 class="title">Holding Kashani</h3><div class="muted">نسخه متصل به سرور مرکزی. فروش رسمی از ثبت‌نام نهایی محاسبه می‌شود و اطلاعات کاربران به‌صورت مشترک ذخیره می‌شود.</div></div></section>`}
function openRequest(type){modal('درخواست '+type,`<div class="field"><label>عنوان / مبلغ / تاریخ</label><input id="rq_title"></div><div class="field"><label>توضیحات</label><textarea id="rq_desc"></textarea></div><button class="btn" onclick="saveRequest('${type}')">ثبت درخواست</button>`)}
function saveRequest(type){state.requests.push({id:'rq'+Date.now(),userId:user().id,type,title:$('#rq_title').value,desc:$('#rq_desc').value,status:'در انتظار بررسی'});save();closeModal();toast('درخواست ثبت شد')}
function openIdea(){modal('ایده خلاقانه',`<div class="field"><label>عنوان ایده</label><input id="idea_title"></div><div class="field"><label>مشکل یا فرصت</label><textarea id="idea_problem"></textarea></div><div class="field"><label>راهکار پیشنهادی و اثر آن</label><textarea id="idea_solution"></textarea></div><button class="btn gold" onclick="saveIdea()">ارسال ایده</button><div class="notice" style="margin-top:10px">ایده‌ای که تأیید و اجرا شود می‌تواند پاداش ویژه و سطح Innovation Award دریافت کند.</div>`)}
function saveIdea(){if(!$('#idea_title').value.trim())return toast('عنوان ایده را وارد کن');state.ideas.push({id:'i'+Date.now(),userId:user().id,title:$('#idea_title').value,problem:$('#idea_problem').value,solution:$('#idea_solution').value,status:'در حال بررسی',reward:'در انتظار'});save();closeModal();toast('ایده ثبت شد')}
function openSuggestion(){modal('انتقادات و پیشنهادات',`<div class="field"><label>نوع</label><select id="s_type"><option>پیشنهاد</option><option>انتقاد</option><option>مشکل</option><option>درخواست بهبود</option></select></div><div class="field"><label>عنوان</label><input id="s_title"></div><div class="field"><label>توضیحات</label><textarea id="s_desc"></textarea></div><div class="field"><label><input id="s_anon" type="checkbox" style="width:auto"> ارسال ناشناس</label></div><button class="btn" onclick="saveSuggestion()">ارسال</button>`)}
function saveSuggestion(){state.suggestions.push({id:'s'+Date.now(),userId:user().id,type:$('#s_type').value,title:$('#s_title').value,desc:$('#s_desc').value,anon:$('#s_anon').checked,status:'ثبت شد'});save();closeModal();toast('ثبت شد')}
function logout(){state.session=null;apiToken='';remoteLoaded=false;localStorage.removeItem(TOKEN_KEY);localStorage.removeItem(SESSION_KEY);localStorage.removeItem(STORAGE_KEY);showDivisionChoice()}
function resetDemo(){toast('بازنشانی عمومی در نسخه سرور غیرفعال است')}
