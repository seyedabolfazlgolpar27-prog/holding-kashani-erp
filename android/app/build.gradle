plugins {
    id 'com.android.application'
}

def serverUrl = project.findProperty('SERVER_URL') ?: System.getenv('SERVER_URL') ?: 'http://93.126.18.48'

android {
    namespace 'com.kashani.holding'
    compileSdk 35

    defaultConfig {
        applicationId "com.kashani.holding.finaltest"
        minSdk 24
        targetSdk 35
        versionCode 220
        versionName "2.2.0-final-test"
        buildConfigField "String", "SERVER_URL", '"' + serverUrl.replace('"','\\"') + '"'
    }

    buildFeatures {
        buildConfig true
    }

    buildTypes {
        debug {
            debuggable true
        }
        release {
            minifyEnabled false
        }
    }
}
